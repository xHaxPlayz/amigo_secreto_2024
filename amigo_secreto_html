<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secret Santa Generator</title>

    <!-- Fonte do Google -->
    <link href="https://fonts.googleapis.com/css?family=Lobster&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: "Lobster", cursive;
            background-color: #f8f9fa;
            text-align: center;
            color: #333;
            margin: 0;
            padding: 0;
        }

        h1 {
            color: #e63946;
        }

        #app {
            margin: 20px auto;
            width: 80%;
            max-width: 700px;
        }

        table {
            margin: 20px auto;
            width: 100%;
            border-collapse: collapse;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }

        table, th, td {
            border: 1px solid #ddd;
        }

        th, td {
            padding: 10px;
            text-align: center;
            background-color: #fff;
        }

        .number {
            font-size: 1.5em;
            color: #e63946;
        }

        button {
            background-color: #e63946;
            color: white;
            border: none;
            padding: 10px 15px;
            font-size: 1em;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #b71c1c;
        }

        @media print {
            button, #instructions {
                display: none;
            }
        }
    </style>
</head>
<body>
    <h1>Gerador de Cartões de Amigo Secreto 🎁</h1>
    <div id="app">
        <div id="instructions">
            <p>Digite o número de participantes e clique em "Gerar Cartões". Imprima a página e corte os cartões!</p>
            <p>Número de participantes: <input type="number" v-model="people" min="2" max="100" /></p>
            <button @click="generateCards">Gerar Cartões</button>
        </div>

        <table v-if="cards.length">
            <tr>
                <th>Você é</th>
                <th>Você está comprando para</th>
            </tr>
            <tr v-for="(card, index) in cards" :key="index">
                <td><span class="number">{{ card.you }}</span></td>
                <td><span class="number">{{ card.giftFor }}</span></td>
            </tr>
        </table>
    </div>

    <!-- Vue.js -->
    <script src="https://cdn.jsdelivr.net/npm/vue@2"></script>
    <script>
        new Vue({
            el: '#app',
            data: {
                people: 0,
                cards: []
            },
            methods: {
                generateCards() {
                    if (this.people < 2) {
                        alert("O número mínimo de participantes é 2.");
                        return;
                    }

                    let numbers = Array.from({ length: this.people }, (_, i) => i + 1);
                    let shuffled = [...numbers];

                    // Embaralhar para garantir que os números sejam diferentes
                    do {
                        shuffled = this.shuffleArray([...numbers]);
                    } while (numbers.some((n, i) => n === shuffled[i]));

                    // Gerar os cartões
                    this.cards = numbers.map((n, i) => ({
                        you: n,
                        giftFor: shuffled[i]
                    }));
                },

                shuffleArray(array) {
                    for (let i = array.length - 1; i > 0; i--) {
                        const j = Math.floor(Math.random() * (i + 1));
                        [array[i], array[j]] = [array[j], array[i]];
                    }
                    return array;
                }
            }
        });
    </script>
</body>
</html>
